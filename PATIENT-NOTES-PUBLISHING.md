# 고민 해결 노트 — 매일 두 편 운영 발행

사용자 승인: 2026-09-17, 현재 디자인과 공감 중심 원고를 승인하고 “매일 2개씩 자동 업로드” 지시. 이 작업의 새 글 작성·커밋·푸시·비디치과 운영 배포는 승인된 범위다. 매번 허락을 다시 요구하지 않는다. 실제 의료진이 검토했다고 꾸미지는 않는다.

## 일정과 중복 방지

- 기준 시간대 Asia/Seoul. 기존 heartbeat ID2 하나가 매일 08:00 작성·예약 배포, 09:00 공개 확인, 10:00 누락 복구를 수행한다. 공개 목표는 매일 09:00이며 새 공개는 하루 두 편으로 유지한다.
- 첫 두 편은 9월 17일 초기 운영 배포에서 공개. 기존 원고 중 다음 두 편은 9월 18일 09:00, 마지막 두 편은 9월 19일 09:00에 공개된다.
- `PatientNote.publishedAt`은 시간대가 명시된 실제 공개 예정 시각이다. 생략하면 초안이다. 운영 목록·상세·관련 링크·사이트맵은 현재 시각이 지난 글만 제공한다. 미리보기에서는 모두 볼 수 있고 noindex이며 분석 태그를 실행하지 않는다.
- `node scripts/patient-notes-status.mjs`를 먼저 실행한다. 날짜별 이미 할당된 글과 운영 URL을 확인한다. 당일 두 편이 할당돼 있으면 추가 작성·발행하지 않고 해당 두 편의 공개 여부를 검증한다. 재실행하거나 배포가 실패해도 같은 두 편으로 재시도한다. 놓친 날짜의 글을 몰아 올리지 않는다.
- 공개 날짜를 매일 바꾸지 않는다. 기존 글 수정은 새 발행 두 편에 포함하지 않으며 SEO를 위해 수정일을 허위 갱신하지 않는다.
- 예약된 글은 서버가 매 요청마다 시각을 확인하므로 추가 빌드 없이 공개된다. 새 원고 작성과 배포는 이 Mac의 Codex 자동화 실행 환경을 사용한다.

## 매일 할 일

1. `/Users/msj/bddc/PROJECT_CONTEXT.md`, `/Users/msj/pfwebengine/PROJECT_CONTEXT.md`, 각 AGENTS.md와 이 문서를 다시 읽는다. 실제 작업 소스는 `/Users/msj/pfwebengine/clients/bdbddc`이며 bddc 루트 Next.js 앱을 수정하지 않는다.
2. `/Users/msj/pfwebengine`에서 `./pfwe check bdbddc`. 원격이 앞서면 공통 지침대로 최신화한다. 다른 작업의 미커밋 변경을 배포에 섞지 않는다. 충돌·갈라짐은 보존하고 보고한다.
3. 발행 상태 스크립트와 운영 `/sitemap-concerns.xml`, 당일 URL을 비교한다. 소스와 운영의 배포 차이를 먼저 해결한다. 소스 할당을 지우고 다른 글로 채우지 않는다.
4. 당일 자리가 남으면 완성된 초안을 우선 사용한다. 없으면 기존 노트·진료 가이드·불편감·컬럼·사이트맵을 검색해 부족한 구체적 상황을 선정하고, 그날 남은 자리 수만큼 새 원고를 작성한다. 지역명만 바꾼 복제 글은 금지한다.
5. 아래 기준으로 원고와 구현을 검토하고 당일 공개 시각을 지정한다. 임상적 판단을 설명하는 문장은 최신 학회·공공기관 등 일차 자료로 확인하고 각 글의 출처 링크를 기록한다. 근거가 부족한 글은 공개하지 않고 이유를 보고한다. 목표 수량 때문에 품질을 낮추지 않는다.
6. 검사: `node scripts/verify-patient-notes.mjs`, `npx tsc --noEmit --skipLibCheck --strict --target ESNext --module ESNext --moduleResolution Bundler --lib ESNext --types @cloudflare/workers-types src/routes/patient-notes.ts`, `npm run build:fast`, `git diff --check`. 새 글의 SSR과 지역·제목·출처·정규 URL·날짜·관련 링크가 모두 맞는지 확인한다. 레이아웃 변경 시 브라우저 모바일/데스크톱도 확인한다.
7. 배포 직전에 원격과 Git 변경 목록을 다시 확인한다. 이번 노트 파일만 변경돼 있다면 `/Users/msj/pfwebengine`에서 `./pfwe push "Publish two patient concern notes YYYY-MM-DD" bdbddc`. 이 명령은 **커밋과 푸시만** 하므로 실제 배포를 생략하지 않는다. 다른 변경이 있으면 정확한 노트 파일만 stage/commit/push하고 다른 변경을 운영에 함께 올리지 않는 방법을 사용한다. reset/clean/강제 push 금지.
8. 검증된 빌드와 커밋으로 실제 사이트 폴더에서 `npx wrangler pages deploy dist --project-name seoul-bd-dental --branch main`. 새 계정·프로젝트·데이터베이스·시크릿은 만들지 않는다. 기존 자격 증명은 출력하거나 문서에 저장하지 않는다.
9. 운영 목록, 공개된 당일 두 URL의 HTTP 200·제목·본문·canonical·index, 사이트맵 포함을 확인한다. 미래 글은 404/noindex와 사이트맵 제외가 맞다. 공개 요청과 배포 로그만으로 성공을 단정하지 않는다. 색인 가능과 검색엔진 색인 완료를 구분한다.
10. 공통 작업 기록에 두 글 제목·URL·확인 시각·커밋·배포 식별자·검증·남은 문제를 기록한다. 신규 발행 성공 또는 실패·사용자 조치가 필요한 변화만 이 작업에 짧게 알린다. 이미 두 편이 공개됐고 상태가 같으면 조용히 종료한다.

## 원고 기준 — 요약보다 공감이 먼저

- 독자가 “내 마음을 아는구나”라고 느끼도록 해당 상황의 막막함, 자책, 치료에 대한 두려움, 비용·시간·이동 부담을 구체적으로 다룬다. 막연한 위로를 반복하거나 불안을 부풀려 예약을 재촉하지 않는다.
- 현재 여섯 편의 구조와 깊이를 기준으로 한다. 공감 도입 → 그 상황의 걱정 네 가지 안팎 → 확인할 부분과 조건별 선택 → 구체적인 후속 질문 여섯 개 안팎 → 상담에 쓸 문장 → 지역 방문 안내. 섹션 수는 내용에 맞게 조절할 수 있다.
- 본문 약 4,500~6,000자 수준의 충분한 설명을 목표로 하되 글자 수를 채우려고 중복 설명하지 않는다. 독립 페이지에서 고민을 충분히 다룬다.
- 모든 글에는 천안·아산·홍성·예산·당진·서산 중 지역 맥락과 불편감/치료/질환이 자연스럽게 들어가야 한다. 기존 지역별 분량을 고려해 고르게 확장한다. 제목에 억지로 지역명을 삽입하지 않는다.
- 각 질문은 고유하고 안정적인 URL 하나를 갖는다. 병원 소재지는 천안 불당동이다. 다른 지역에 실제로 없는 분원을 표시하지 않는다.
- 실제 환자 후기·경험·의료진 인용을 만들어내지 않는다. 보증·보험·비용·소요 시간·당일 완료·완치 등을 근거 없이 약속하지 않는다. 집에서 진단하거나 직접 처치하도록 안내하지 않는다. 증상만으로 발치·수술·재치료를 확정하지 않는다.
- 기존 진료 페이지·관련 노트·지역 안내로 문맥에 맞게 연결한다. 이미 답이 충분한 주제는 새 글 대신 보완 후보로 남기고 다른 구체적 고민을 선택한다.
- 기존 site-v5 디자인, ssrHeader/ssrMobileNav, 정확한 병원 푸터를 유지한다. 주 메뉴에 노트를 추가하지 않는다. 필터는 noindex, 대표 URL은 사이트맵에 포함한다. Amplitude는 영구 금지다.

## 수정 위치

- `src/data/patient-notes.ts`: 메타데이터, 지역, 기본 안내·출처·관련 링크와 publishedAt.
- `src/data/patient-note-details.ts`: 공감 도입, 상황별 설명, 추가 질문, 상담 문장, 마무리.
- `src/routes/patient-notes.ts`: SSR·시각별 공개 제어. 통상 매일 수정할 필요 없음.
- `scripts/verify-patient-notes.mjs`: 글이 늘어도 동작하는 검증과 KST 발행 경계 검사.
- `scripts/patient-notes-status.mjs`: 소스 기준 날짜별 발행 자리와 운영 검증 대상 URL. 이를 운영 배포 성공 기록으로 오해하지 않는다.

초기 질문 후보: `/Users/msj/bddc/reports/2026-09-16-website-analysis/NICHE-CONTENT-TREE.md`와 `SEO-TECH-TREE.md`. 후보는 실제 수요나 임상 답변을 확인한 완성 원고가 아니므로 중복·근거를 매번 확인한다.


## 검색엔진 통보와 성과 점검 (2026-09-17 추가 승인)

사용자 요청: 운영 공개 이후 검색 유입에 필요한 조치를 모두 실행하고 성과도 예측한다. 기존 오전9시 발행 작업에서 다음을 수행한다.

- 공개 검증이 끝나면 `node scripts/notify-patient-notes.mjs --state /Users/msj/bddc/reports/2026-09-17-notes-search-launch/indexnow-state.json --submit` 실행. 운영 사이트맵에 실제 공개된 새 노트와 변경된 허브만 대상으로 네이버와 Bing에 IndexNow 통보한다. 예약 글은 제외한다. 첫 실행 전 `--submit` 없이 대상을 검토할 수 있다.
- 200은 접수, 202는 접수 및 키 확인 대기다. 이를 검색 색인 완료라고 표현하지 않는다. 엔진별 영속 기록으로 이미 접수된 동일 글은 재전송하지 않는다. 실패한 엔진만 다음 실행에 재시도한다. 스크립트 오류·429 시 반복 호출하지 말고 기록한다. 과거 글의 내용 변경은 이 신규 공개 전용 스크립트와 별도로 변경 URL만 통보한다.
- 오늘 두 편이 이미 공개된 경우에도 검색엔진 통보 기록이 비어 있으면 누락된 통보를 처리한다. 소스 공개 여부와 운영 검증·검색엔진 접수·검색 색인을 구분해 기록한다.
- Google은 제출된 `/sitemap-concerns.xml`을 다시 읽는 경로를 유지한다. 매일 같은 사이트맵·URL을 수동 재제출하지 않는다. 새 노트 색인 문제가 지속될 때만 URL 검사를 통해 원인을 확인하고 필요한 URL을 요청한다. Google Indexing API를 일반 진료 콘텐츠에 사용하지 않는다.
- 첫 색인 확인은 다음 정시 실행에서 읽기 전용 Search Console URL Inspection으로 공개 노트만 확인한다. 이후 매주 목요일에 14일 이상 된 미색인 글을 우선 최대10개 표본으로 점검한다. 인증·권한이 없으면 조작하거나 결과를 꾸미지 않고 기록한다.
- 매주 목요일 GSC 최신 확정 28일의 `/concerns/` 클릭·노출·검색어와 기존 가이드/지역 페이지를 비교한다. 처음 28일은 불완전 관측 기간임을 표시한다. 핵심 지역 검색어는 천안·아산·홍성·예산·당진·서산이며, 노트별 공개 후 경과일도 함께 본다. URL 증가율을 트래픽 증가율로 간주하지 않는다.
- 성과 원시 자료와 제출 결과는 `/Users/msj/bddc/reports/2026-09-17-notes-search-launch/` 아래 날짜별로 남긴다. 기존 원시 자료를 덮어쓰지 않는다. 사용 가능한 기존 읽기 전용 Google 서비스 계정은 `/Users/msj/bddc/content-pipeline/daily-watch/gcp-sa.json`이며 값은 출력·복사하지 않는다. GSC 속성은 `sc-domain:bdbddc.com`이다. URL-prefix 속성의 API 권한은 별도이므로 도메인 속성으로 조회한다.
- GA4는 `/concerns/` 진입 세션·참여·예약/전화/카카오 행동을 분리한다. 기존 중복 이벤트 합계를 실제 상담·내원 수로 환산하지 않는다. 실제 접수와 연결되지 않으면 신규 환자·매출은 예측하지 않는다.
- 전망은 공개 글 수 × 색인 가정 × 글당 클릭 가정으로 계산한 시나리오이며 보장치가 아니다. 30·60·90일 실측으로 갱신하고 기존 페이지 클릭을 가져온 효과와 사이트 전체 순증을 구분한다. 효과가 약한 주제는 양을 늘리기보다 의도 중복·본문·내부 링크를 보완한다.

공식 기준: https://developers.google.com/search/docs/crawling-indexing/ask-google-to-recrawl · https://searchadvisor.naver.com/guide/indexnow-request

- 2026-09-17 사용자 정정: 네이버 서치어드바이저는 이미 다른 아이디로 등록되어 있다. 현재 브라우저의 비디문원장 계정에 새 소유권을 추가하지 않는다. 로그인 계정에서 사이트 목록이 비어 있다는 이유로 사이트 미등록을 단정하지 않는다. 기존 인증 태그를 유지하며, 관리 화면 확인이 필요할 때는 기존 등록 계정으로 진행한다. IndexNow 수집 통보는 기존 계정 로그인 없이 현재 절차대로 계속한다.


## 자동 실행 누락 방지 (2026-09-20 복구)

- 9/20 09시 실행은 시작됐으나 파일 확인 후 원고 작성·배포 전에 일반 대화 응답으로 종료됐다. 예약 활성화와 실행 시작은 발행 성공이 아니다.
- 08시 실행: 당일 자리가 부족하면 새 원고를 작성·검증하고 당일09시로 예약해 기존 운영에 배포한다. 이미 할당된 글을 재사용한다. 예약 배포 직후 `node scripts/verify-patient-notes-live.mjs --allow-scheduled --output <새-감사파일.json>`으로 기존 공개분·사이트맵·예약 글404/noindex를 확인한다. 결과 `scheduled`는 예약 준비이며 공개 완료로 보고하지 않는다.
- 09시 실행: `node scripts/verify-patient-notes-live.mjs --output <새-감사파일.json>`의 `stage: published`와 `allPassed: true`를 확인하고 미접수 IndexNow를 처리한다. 실패하거나 할당이 부족하면 이번 실행에서 보완·배포·검증한다.
- 10시 실행: 같은 두 URL의 누락·배포 차이·미접수를 다시 확인해 필요한 부분만 복구한다. 이미 정상 공개·접수됐으면 새 글·재배포·중복 통보 없이 종료한다.
- 발행 점검기는 당일 할당이0/1편이면 실패한다. 과거 글만 정상인 상태, 새 글404, noindex, 사이트맵 불일치를 성공으로 처리하지 않는다. 목록25편 이상에서도 페이지 나눔을 따라 당일 글 연결을 확인한다.
- 현재 실행 날짜·두 slug·작성/검증/배포 단계·다음 행동을 공통 기록의 진행 중 작업에 남긴다. 대화 압축·재개 후 반드시 이 기록과 당일 상태를 읽고 미완료 발행을 이어간다. 이전 칭찬이나 상태 질문에 답한 것만으로 정기 작업을 끝내지 않는다.
- 09시 이후 지연 작성은 실제 공개 시각을 사용한다. 놓친 날짜를 소급하거나 하루 두 편을 초과하지 않는다. 실패하면 실패 단계와 남은 일을 기록·보고하고 완료라고 표시하지 않는다.
- 검증기 회귀 검사: `node scripts/test-patient-notes-live.mjs`. 작성/공개는 이 Mac의 Codex 자동화 실행에 의존한다. 이미 운영에 배포된 예약 글의09시 공개는 서버가 수행한다.

## 2026-09-21 측정·내부 링크 개선 이후 운영

- 하루 두 편과 공감 중심 원고 원칙을 유지한다. 새 글은 기존 가이드 또는 비교·후회 가이드 중 적합한 URL을 `related`에 최소 하나 포함한다. 가이드/6지역 안내는 `src/lib/patient-journey.ts`가 공개된 노트만 최신순 최대6개 연결한다. 미래 글은 안내 링크에도 노출하지 않는다. 오래된 노트는 노트 허브/페이지 나눔에서 계속 접근 가능하다.
- 모든 상담 클릭은 `public/static/bd-conversions.js`가 담당한다. 페이지별 인라인 gtag나 별도 클릭 리스너를 추가하지 않는다. 기존 두 GA 속성과 Clarity는 유지한다.
- 전화 `phone_call_click`, 카카오 `kakao_click`, 네이버 이동 `naver_booking_click`, 상담 화면 이동 `reservation_click`은 각각 의향이다. 합계를 환자 수/예약 수로 표시하지 않는다. `generate_lead`는 `/api/reservation`의 개별 저장 성공 응답 직후 1회만 발화하며 실제 예약 확정·내원과 다르다. `contact`·`reservation_complete` 구 중복 발화는 중단했다. 과거 데이터는 삭제하거나 새 정의로 소급하지 않는다.
- 새 측정 정의의 첫 완전한 한국 날짜는 2026-09-22다. `content_read_complete`는 화면 활성30초+75%스크롤 조건이며 읽기/이해 완료의 증명이 아니다. CTA 노출은 `consultation_cta_view`.
- 매주 목요일09시 점검에서 `node scripts/collect-consultation-performance.mjs --output /Users/msj/bddc/reports/2026-09-17-notes-search-launch/YYYY-MM-DD-performance`로 GA4/GSC 집계를 새 디렉터리에 수집한다. 동일 보고서가 있으면 재실행/덮어쓰지 않고 확인한다. 첫 7/28/56일은 발행일 포함 달력 날짜 기준; 미성숙 기간은 관측 중으로 표시한다. 검색어는 기존 GSC 별도 점검을 유지한다.
- 2026-10-08 목요일09시에는 새 정의의 첫14일(9/22~10/5)을 3일 처리 지연 뒤 검토한다. 랜딩 미상 비율, 공통 클릭별 추이, 폼 저장 성공, Clarity 오류를 9/21 기준 보고서와 대조한다. 이전/이후 이벤트 정의 차이와 지역·기기 구성을 함께 설명하고 수치 하락을 즉시 사업 악화로 해석하지 않는다. Clarity는 그 시점72시간과 이전72시간 스냅샷을 구분한다.
- 수집 스크립트는 기존 읽기 전용 인증만 사용한다. API 오류·행 제한·샘플링·임계값 제한은 기록한다. 예약 원문·환자 정보는 분석 파일에 포함하지 않는다. 미설정 랜딩이나 동의·차단으로 유실된 과거 방문을 복구했다고 말하지 않는다.
