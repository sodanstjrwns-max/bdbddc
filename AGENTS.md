# 비디치과 사이트별 작업 규칙

상위 `/Users/msj/pfwebengine/AGENTS.md`의 공유·Git 작업 지침을 함께 따른다.

## 리뷰 표시 방식 (2026-09-21 사용자 지시)

사용자는 보건소 안내에 따라 병원 홈페이지에서 리뷰·별점·만족도를 직접 노출하지 않고, 기존처럼 네이버와 구글의 외부 링크에서 확인하도록 요청했다.

- 병원 리뷰 본문, 환자의 치료 경험을 인용한 홍보문, 별점·평점·리뷰 수·만족도 수치를 공개 페이지에 넣지 않는다. 다국어·메타데이터·JSON-LD·AI 상담·생성 템플릿도 같은 기준이다.
- 병원에 대한 `Review` / `AggregateRating` 구조화 데이터도 만들지 않는다.
- 기존 네이버·구글 외부 링크는 유지한다. 공통 생성 함수와 확인된 목적지는 `scripts/review-links.cjs`에 있다. 임의로 다른 병원의 장소 ID로 바꾸지 않는다.
- 환자분의 고민을 가정한 질문, 일반적인 치료 설명, 의료진의 진료 철학, 학술 논문의 review는 병원 후기가 아니다. 단어만 보고 삭제하지 않는다.
- `npm run build:fast`의 리뷰 검사에 실패하면 원본을 수정한다. 검사를 우회하거나 CSS로만 가리지 않는다.

이 규칙은 사용자의 운영 지시를 기록한 것이며 법률 적합성을 보증하는 문구가 아니다.
