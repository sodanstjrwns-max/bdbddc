# 병원 홈페이지 제작 기획서 (완전판)

## Patient Funnel Standard Template

> **작성자**: Patient Funnel (페이션트 퍼널)
> **버전**: v3.0 — 범용 템플릿
> **최종 수정**: 2026-02-19
> **레퍼런스 사이트**: https://bdbddc.com (서울비디치과)

---

# 변수 정의표

> 아래 변수를 각 병원 정보로 치환하면 해당 병원 전용 기획서가 됩니다.
> 기획서 본문에서 `{변수명}` 형태로 사용됩니다.
> **예시 값**은 서울비디치과(bdbddc.com) 기준입니다.

## 기본 정보

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{병원명}` | 병원 한글 이름 | 서울비디치과 |
| `{병원명_영문}` | 병원 영문 이름 | Seoul BD Dental |
| `{대표원장}` | 대표원장 이름 | 문석준 |
| `{도메인}` | 홈페이지 도메인 | bdbddc.com |
| `{전화번호}` | 대표 전화번호 | 041-415-2892 |
| `{주소}` | 병원 상세 주소 | 충남 천안시 서북구 불당25로 60, 8~10층 |
| `{우편번호}` | 우편번호 | 31168 |
| `{위도}` | GPS 위도 | 36.8151 |
| `{경도}` | GPS 경도 | 127.1139 |
| `{지역코드}` | 행정구역 코드 (ISO 3166-2:KR) | KR-44 |
| `{도시명}` | 소재 도시 | 천안시 |
| `{도명}` | 소재 도/광역시 | 충청남도 |
| `{동네명}` | 소재 동/읍/면 | 불당동 |

## 브랜드·디자인

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{프라이머리컬러}` | 브랜드 대표 색상 (HEX) | #C9A962 (골드) |
| `{세컨더리컬러}` | 보조 색상 (HEX) | #0369a1 (블루) |
| `{다크배경}` | 다크모드 배경색 | #0a0908 |
| `{메인카피}` | 히어로 헤드라인 | 과잉진료 없는 양심치과 |
| `{서브카피}` | 히어로 서브라인 | 서울대 15인 원장 협진 · 365일 진료 |
| `{브랜드톤}` | 디자인 톤 (택1) | 프리미엄 |

## 진료 정보

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{진료과}` | 진료과 (치과/피부과/한의원 등) | 치과 |
| `{의사수}` | 전체 의사 수 | 15 |
| `{주력치료}` | 주력 치료 TOP 5 | 임플란트, 교정, 소아치과, 심미치료, 글로우네이트 |
| `{전체치료목록}` | 전체 치료/진료 항목 | 임플란트, 인비절라인, 소아치과, 심미치료, 글로우네이트, 충치, 레진, 크라운, 인레이, 신경치료, 미백, 스케일링, 잇몸치료, 치주염, 사랑니, 턱관절, 이갈이 (24종) |
| `{치료카테고리}` | 치료 분류 체계 | 전문센터 / 일반·보존 / 잇몸·외과 |
| `{자체브랜드치료}` | 병원 고유 브랜드 치료 (없으면 빈칸) | 글로우네이트 (Glownate) |
| `{진료시간_평일}` | 평일 진료시간 | 09:00 ~ 20:00 |
| `{진료시간_토}` | 토요일 진료시간 | 09:00 ~ 17:00 |
| `{진료시간_일}` | 일요일 진료시간 | 10:00 ~ 16:00 |
| `{진료시간_공휴일}` | 공휴일 진료시간 | 10:00 ~ 16:00 |
| `{365일진료여부}` | 365일 진료 여부 | Yes |

## 차별점

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{차별점1}` | 핵심 차별점 #1 | 서울대 출신 15인 원장 협진 |
| `{차별점2}` | 핵심 차별점 #2 | 365일 진료 (일요일·공휴일 포함) |
| `{차별점3}` | 핵심 차별점 #3 | 6개 독립 수술실 |
| `{차별점4}` | 핵심 차별점 #4 | 원내 기공소 운영 |
| `{차별점5}` | 핵심 차별점 #5 | 디지털 정밀 진단 (0.1mm 단위) |
| `{차별점6}` | 핵심 차별점 #6 | 에어샤워 감염관리 시스템 |
| `{시설규모}` | 병원 규모 (평수 등) | 400평 |
| `{수술실수}` | 수술실/시술실 수 | 6개 |

## 외부 서비스

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{네이버예약URL}` | 네이버 예약 링크 | https://naver.me/5yPnKmqQ |
| `{카카오톡URL}` | 카카오톡 채널 링크 | https://pf.kakao.com/_Cxivlxb |
| `{유튜브URL}` | 유튜브 채널 | (해당 시 입력) |
| `{인스타그램URL}` | 인스타그램 | (해당 시 입력) |
| `{블로그URL}` | 블로그 (네이버/InBlog 등) | (해당 시 입력) |
| `{구글PlaceID}` | Google Place ID | (해당 시 입력) |
| `{구글API키}` | Google Places API Key | (해당 시 입력) |
| `{네이버평점}` | 네이버 리뷰 평점 | 4.85 |
| `{구글평점}` | 구글 리뷰 평점 | 4.9 |
| `{리뷰수}` | 총 리뷰 수 | 500+ |

## 지역 SEO

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{서비스지역목록}` | 타겟 지역 리스트 | 천안, 아산, 불당, 세종, 대전, 청주, 평택, 공주, 당진, 서산, 논산, 예산, 홍성, 충주, 진천, 안성 (16개) |
| `{지역페이지수}` | 지역 페이지 수 | 16 |

## 다국어

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{다국어목록}` | 번역 언어 | 영어, 베트남어, 중국어, 러시아어, 일본어 |
| `{다국어수}` | 번역 언어 수 (원어 제외) | 5 |
| `{Weglot플랜}` | Weglot 요금제 | Pro (EUR 79/월) |

## 규모 지표

| 변수명 | 설명 | 예시 값 |
|--------|------|---------|
| `{총페이지수}` | HTML 페이지 총 수 | 80 |
| `{치료페이지수}` | 치료 상세 페이지 수 | 24 |
| `{의사프로필수}` | 의사 개별 페이지 수 | 15 |
| `{JSON-LD수}` | 구조화 데이터 항목 수 | 134 |
| `{사이트맵URL수}` | sitemap.xml URL 수 | 73 |

---

# 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [사이트 구조 설계](#2-사이트-구조-설계)
3. [페이지별 상세 기획](#3-페이지별-상세-기획)
4. [UI/UX 설계 기준](#4-uiux-설계-기준)
5. [SEO 인프라](#5-seo-인프라)
6. [구조화 데이터 (Schema.org)](#6-구조화-데이터-schemaorg)
7. [마케팅 & 전환 인프라](#7-마케팅--전환-인프라)
8. [다국어 지원](#8-다국어-지원)
9. [기술 스택 & 아키텍처](#9-기술-스택--아키텍처)
10. [검색엔진 등록 & 색인](#10-검색엔진-등록--색인)
11. [법적 필수 요소](#11-법적-필수-요소)
12. [성능 & 보안](#12-성능--보안)
13. [납품 체크리스트](#13-납품-체크리스트)
14. [운영 가이드](#14-운영-가이드)
15. [부록: 커스터마이징 옵션](#15-부록-커스터마이징-옵션)

---

# 1. 프로젝트 개요

## 1.1 이 기획서의 목적

이 문서는 단순한 홈페이지 제작 매뉴얼이 아닙니다. 환자가 병원을 검색하는 순간부터 진료를 마치고 지인에게 소개하기까지, 그 전체 여정을 하나의 디지털 시스템으로 설계하기 위한 청사진입니다.

병원 홈페이지는 "있으면 좋은 것"이 아니라 "없으면 존재하지 않는 것"입니다. 2026년 현재, 환자의 87%가 병원 방문 전 온라인 검색을 하고, 그중 73%가 홈페이지를 통해 최종 결정을 내립니다. 네이버 플레이스와 카카오맵의 기본 정보만으로는 환자의 깊은 고민에 답할 수 없습니다. "이 병원이 정말 잘하는가?", "내 상황에 맞는 치료법은 무엇인가?", "비용은 얼마나 드는가?", "어떤 의사가 담당하는가?" — 이런 질문에 설득력 있게 답하는 홈페이지를 가진 병원만이 환자의 예약 버튼 클릭을 이끌어냅니다.

이 기획서는 `{병원명}`의 홈페이지(https://`{도메인}`)를 구축하기 위한 완전한 설계도입니다. 레퍼런스 사이트인 서울비디치과(bdbddc.com)에서 실제로 검증한 구조와 전략을 기반으로, `{병원명}`의 특성에 맞게 적용합니다. 단순히 "이런 페이지가 필요합니다"라는 목록이 아니라, 왜 그 페이지가 필요한지, 그 페이지가 환자 퍼널의 어느 단계에서 어떤 역할을 하는지, 그리고 어떻게 만들어야 실제로 전환이 일어나는지를 상세하게 설명합니다.

## 1.2 적용 대상

이 기획서는 `{진료과}`를 기준으로 작성되었으며, 의료기관 전반에 적용할 수 있습니다. 치과는 치료 종류가 다양하고(임플란트부터 스케일링까지), 비급여 비율이 높아 환자의 온라인 비교 행동이 가장 활발한 진료과이기 때문에 기획서의 밀도가 자연스럽게 높아집니다. 피부과와 성형외과는 비포/애프터 갤러리와 비주얼 마케팅을 강화하면 그대로 적용됩니다. 내과와 일반의원은 치료 페이지를 진료과목 페이지로 전환하고 건강검진 예약 시스템을 추가하면 됩니다. 한의원은 한방 치료 원리 설명 콘텐츠와 체질 진단 퀴즈 같은 리드 마그넷을 접목하면 차별화됩니다. 안과와 이비인후과는 치료 항목이 상대적으로 적으므로 구조를 간소화해 적용합니다. 다만 종합병원은 진료과 수와 조직 구조가 워낙 크기 때문에 별도 설계가 필요합니다.

## 1.3 핵심 철학: 환자 퍼널 10단계

> **"환자가 병원을 인지하는 순간부터 지인에게 소개하기까지의 전체 여정을 설계한다"**
> — Patient Funnel (페이션트 퍼널)

홈페이지의 모든 구조와 기능은 이 퍼널의 각 단계에 대응합니다. 단계별로 살펴보겠습니다.

**1단계: 인지** — 환자는 "`{도시명}` `{주력치료 첫 번째}`", "`{동네명}` `{진료과}`" 같은 검색어로 병원을 발견합니다. 이 단계에서 홈페이지가 해야 할 일은 검색엔진에서 노출되는 것입니다. 지역 SEO 페이지 `{지역페이지수}`개, 치료 페이지 `{치료페이지수}`개, `{JSON-LD수}`개의 구조화 데이터가 이 단계를 담당합니다.

**2단계: 관심** — 검색 결과를 클릭해 홈페이지에 들어온 순간, 첫 3초가 승부입니다. 히어로 섹션의 메인 카피("`{메인카피}`"), 운영 상태 배지("오늘 `{진료시간_평일의 종료시간}`까지 진료중"), 핵심 통계("`{서브카피}`")가 환자의 이탈을 막습니다.

**3단계: 탐색** — 관심을 가진 환자는 자신에게 해당하는 치료 정보를 찾습니다. `{치료페이지수}`개의 치료 상세 페이지가 "이 증상이면 이런 치료를 합니다"라는 맞춤형 답변을 제공합니다. 각 페이지에는 고민 공감 섹션, 치료 종류, 과정 설명, FAQ가 포함됩니다.

**4단계: 비교** — 환자는 반드시 다른 병원과 비교합니다. "왜 `{병원명}`이어야 하는가?"에 답하기 위해 의사 프로필 `{의사프로필수}`개(학력, 경력, 전문 분야, 소개 영상), 시설 안내(층별 가이드), 차별점 카드(`{차별점1}`, `{차별점2}`, `{차별점3}` 등)가 준비되어야 합니다.

**5단계: 신뢰** — 비교를 마친 환자는 "정말 잘하는가?"를 확인합니다. 실시간 구글 리뷰 위젯(API 연동), 비포/애프터 갤러리(슬라이더 비교), 외부 리뷰 플랫폼 링크가 사회적 증거를 제공합니다.

**6단계: 결심** — 신뢰가 쌓인 환자는 예약을 결심합니다. 이 순간을 놓치지 않기 위해 모든 페이지에 CTA(전화 `{전화번호}`, 카카오톡, 네이버 예약, 온라인 폼)가 배치됩니다. 모바일 하단 고정 바, 데스크톱 플로팅 버튼, 페이지 내 중간/하단 CTA 섹션이 항상 손 닿는 곳에 있어야 합니다.

**7단계: 방문** — 예약한 환자가 실제로 찾아오게 하는 것도 홈페이지의 역할입니다. 오시는 길 페이지에 자가용/대중교통/주차 정보, 구글맵·네이버맵·카카오맵 연동, 건물 사진이 포함됩니다.

**8단계: 경험** — 실제 진료는 오프라인 영역이지만, 대기실에서 "우리 병원 Wi-Fi 연결" 안내, 모바일 예약 확인 등 디지털 접점이 경험을 보완합니다.

**9단계: 만족** — 진료를 마친 환자에게 리뷰 작성 유도 링크(구글/네이버/카카오)를 안내합니다. QR코드를 통한 리뷰 페이지 연결도 효과적입니다.

**10단계: 소개** — 만족한 환자가 지인에게 추천할 때, 공유하기 쉬운 URL 구조, Open Graph 이미지(1200×630), 소셜 공유 버튼이 바이럴을 촉진합니다.

---

# 2. 사이트 구조 설계

## 2.1 전체 구조 — 왜 `{총페이지수}`페이지인가

"병원 홈페이지에 `{총페이지수}`페이지가 필요한가?"라는 질문을 자주 받습니다. 답은 간단합니다. 환자의 질문이 그만큼 많기 때문입니다. "`{주력치료 첫 번째}`는 아프나요?", "`{서비스지역목록의 두 번째}`에서 얼마나 걸리나요?", "`{대표원장}` 원장 경력이 어떻게 되나요?" — 이 모든 질문에 전용 페이지로 답하면, 각 페이지가 검색엔진에서 독립적으로 노출됩니다. 하나의 메인 페이지에 모든 정보를 쑤셔넣으면 "`{도시명}` `{주력치료 첫 번째}`"를 검색한 환자에게도, "`{서비스지역 다른 도시}` `{진료과}`"를 검색한 환자에게도 똑같은 페이지가 뜨는데, 이것은 환자가 원하는 답이 아닙니다.

페이지 구성은 다음과 같습니다. 핵심 정보 페이지 5개(메인, 예약, 오시는 길, 비용, 의료진 목록), 치료 상세 페이지(치료 항목 수 + 전체 목록 1개), 의사 프로필(`{의사프로필수}`개), 지역 SEO 랜딩 페이지(`{지역페이지수}`개), 콘텐츠/신뢰 페이지(갤러리, 칼럼, 영상, FAQ, 치료별 FAQ), 병원 정보/법적 페이지(미션, 층별 안내, 공지, 개인정보처리방침, 이용약관, 404), 회원/관리 시스템(로그인, 회원가입, 마이페이지, 관리자). 이 구조는 기준이며, `{진료과}`의 특성과 규모에 맞게 조정합니다. 치료 항목이 적은 병원은 30~40페이지로 시작하고, 규모가 큰 병원은 100페이지 이상으로 확장합니다.

## 2.2 페이지 우선순위 — 어디에 집중할 것인가

모든 페이지의 중요도가 같지 않습니다.

**레벨 1: 핵심 페이지 (5개)** — 사이트의 뼈대입니다. 메인 페이지(/)는 병원의 첫인상이자 모든 트래픽의 허브입니다. 예약/상담 페이지(/reservation)는 전환의 최종 목적지입니다. 오시는 길(/directions)은 방문 결심을 굳히는 마지막 페이지입니다. 비용 안내(/pricing)는 환자가 가장 궁금해하면서도 대부분의 병원이 제대로 공개하지 않는 정보입니다 — 이것을 투명하게 공개하는 것만으로 신뢰를 크게 얻습니다. 의료진 목록(/doctors)은 "누가 나를 치료하는가?"라는 가장 본질적인 질문에 답합니다.

**레벨 2: 치료/진료 페이지 (`{치료페이지수}`개)** — 매출에 직결됩니다. 환자가 "`{주력치료 첫 번째}` 잘하는 `{진료과}`"를 검색했을 때, 전용 페이지가 없으면 경쟁에서 밀립니다. `{병원명}` 기준으로 `{주력치료}`가 가장 중요합니다. 치료 페이지는 `{치료카테고리}` 체계로 분류합니다.

**레벨 3: 의사 프로필 (`{의사프로필수}`개)** — 신뢰 구축의 핵심입니다. 환자는 "이 의사가 어디를 나왔고, 경력이 몇 년이고, 어떤 분야를 전문으로 하는지"를 반드시 확인합니다.

**레벨 4: 지역 SEO 페이지 (`{지역페이지수}`개)** — "`{도시명}` `{진료과}`", "`{서비스지역의 다른 도시}` `{주력치료}`" 등 지역+치료 키워드를 잡기 위한 전략 페이지입니다. `{병원명}`은 `{서비스지역목록}` 등 `{지역페이지수}`개 지역을 타겟합니다. 지역 선정 기준은 1순위가 병원 소재 도시 내 주요 동/구, 2순위가 인접 도시(차로 15~20분), 3순위가 차로 30분 거리, 4순위가 차로 1시간 거리(이 경우 고단가 전문 치료에 한해 설득력이 있습니다)입니다.

## 2.3 네비게이션 — 환자가 길을 잃지 않게

`{총페이지수}`페이지를 가진 사이트에서 환자가 원하는 정보를 3클릭 이내에 찾게 하려면 네비게이션 설계가 결정적입니다.

**GNB(Global Navigation Bar)**는 다음과 같이 구성합니다. 좌측에 로고, 중앙에 메인 메뉴(진료안내 드롭다운, 의료진, 미션·비전, 층별안내, 오시는 길, FAQ, 공지), 우측에 액션 버튼(전화 `{전화번호}`, 로그인/가입, 예약하기)을 배치합니다. 진료안내 드롭다운은 `{치료카테고리}` 체계에 따라 분류합니다. 이 구조가 중요한 이유는 환자가 "나는 어디가 아픈데 어디를 봐야 하지?"라고 생각할 때, 직관적으로 해당 카테고리를 찾을 수 있기 때문입니다.

**모바일 하단 고정 CTA Bar**는 전화, 카카오톡, 예약하기(강조), 오시는 길 4개 버튼으로 구성합니다. 이 바는 스크롤해도 항상 화면 하단에 고정되어야 합니다. 모바일 트래픽이 전체의 75% 이상을 차지하는 상황에서, 환자가 "지금 전화하고 싶다"고 느끼는 순간 엄지손가락 하나로 터치할 수 있어야 합니다.

**데스크톱 플로팅 CTA**는 화면 우측 하단에 맨 위로 이동, 카카오톡 상담, 전화 상담 3개 아이콘을 세로로 배치합니다.

CTA 배치의 원칙은 "환자가 어떤 페이지의 어떤 위치에서 스크롤을 멈추더라도, 시야 안에 최소 1개의 CTA가 보여야 한다"입니다. 구체적으로 헤더에 전화+예약 버튼(필수), 히어로 섹션에 상담 예약+전화(필수), 본문 중간에 네이버 예약+카카오(권장), 하단 CTA 섹션에 네이버 예약+카카오+전화(필수), 모바일 하단 고정 바에 전화+카카오+예약+길찾기(필수), 데스크톱 플로팅에 카카오+전화+맨위로(권장), 푸터에 전화+주소+소셜(필수)을 배치합니다.

---

# 3. 페이지별 상세 기획

## 3.1 메인 페이지 — 3초 안에 승부를 결정짓는 곳

메인 페이지는 환자가 병원 홈페이지에서 가장 처음 보는 화면이자, 전체 사이트에서 가장 많은 트래픽을 받는 페이지입니다. 이 페이지의 목표는 단 하나 — 환자가 "여기 괜찮겠는데?"라고 느끼고 다음 행동(치료 페이지 클릭, 예약, 전화)으로 넘어가게 하는 것입니다.

메인 페이지는 총 13개 섹션으로 구성합니다. 순서가 중요합니다. 환자가 스크롤하면서 자연스럽게 퍼널을 따라 내려가도록 설계합니다.

**첫 번째, 히어로 섹션**은 페이지의 얼굴입니다. 운영 배지("오늘 `{진료시간_평일의 종료시간}`까지 진료중")는 실시간성을 줘서 긴박감을 만듭니다. 환자가 "지금 전화해도 되는 곳이구나"라고 즉각 인지합니다. 서브타이틀로 지역(예: `{도시명}` `{동네명}`)을 명시하고, 메인 카피는 병원의 핵심 가치를 한 문장으로 압축합니다 — "`{메인카피}`". 그 아래에 핵심 통계 배지(`{차별점1}` | `{차별점2}` | `{차별점3}` 등)를 배치해 한눈에 차별점을 전달합니다. CTA 버튼은 주 CTA(상담 예약하기)와 보조 CTA(비용 안내)를 나란히 놓고, 최하단에 리뷰 통계(네이버 `{네이버평점}` / 구글 `{구글평점}`)를 넣어 사회적 증거를 즉시 제공합니다.

히어로 카피라이팅의 공식이 있습니다. 환자의 고민을 대변하는 헤드라인 + 우리가 해결하겠다는 약속으로 구성합니다. `{진료과}`가 치과인 경우 예를 들면: "다른 곳에서 안 된다고요?" → "저희가 해드리겠습니다"(임플란트), "교정은 아프고 티나잖아요" → "투명하게, 편안하게"(교정), "우리 아이가 치과를 무서워해요" → "놀이처럼 즐거운 진료"(소아치과). 이 공식은 메인 페이지뿐 아니라 모든 치료 페이지 히어로에도 동일하게 적용합니다.

**두 번째, 운영 상태 배지** — "오늘 `{진료시간_평일의 종료시간}`까지 진료" 등 현재 상태를 보여주는 작은 배지입니다. `{365일진료여부}`가 Yes인 경우 "365일 진료"를 함께 표시합니다. 이것의 효과는 "이 사이트는 살아있는 사이트구나, 관리되고 있구나"라는 인상을 줍니다.

**세 번째, 핵심 차별점 카드** — 3~6개의 키워드를 카드 형태로 보여줍니다. `{차별점1}`, `{차별점2}`, `{차별점3}`, `{차별점4}`, `{차별점5}`, `{차별점6}` 등입니다. 각 카드는 아이콘 + 숫자/키워드 + 한 줄 설명으로 구성합니다. 환자가 이 섹션을 훑어보는 데 5초면 충분하고, 5초 만에 "여기는 규모가 있고, 전문적이고, 꼼꼼한 병원이구나"라는 인상을 심습니다.

**네 번째부터 열세 번째까지**는 주력 치료 소개(4~6개 카드), 의료진 소개(`{대표원장}` + 주요 의사 3~5명), Before/After 슬라이더, 구글/네이버 리뷰 위젯, 실시간 구글 리뷰 API 연동, 비용 미리보기, FAQ 미리보기(3~5개), 최신 칼럼/영상, 최종 CTA("지금 바로 상담 받아보세요"), 푸터 순서로 이어집니다. 각 섹션은 환자 퍼널의 관심→탐색→비교→신뢰→결심 순서를 따릅니다.

## 3.2 치료 상세 페이지 — 환자의 불안을 지식으로 바꾸는 공간

치료 페이지는 사이트에서 가장 중요한 수익 직결 페이지입니다. `{치료페이지수}`개 모든 치료 페이지는 동일한 구조를 따르되, 내용과 톤은 각 치료의 특성에 맞게 달라집니다.

첫 번째 섹션은 히어로입니다. 감성적인 카피로 시작하되, 바로 아래에 치료 정의 설명박스를 배치합니다. 환자의 감정에 공감하면서도 즉시 정확한 정보를 제공하는 것이 핵심입니다.

두 번째 섹션은 고민 공감입니다. "혹시 이런 고민이 있으신가요?"라는 질문과 함께 3~4가지 일상적인 고민을 나열합니다. 이 섹션의 효과는 "아, 여기 사람들이 내 마음을 정확히 알고 있구나"라는 공감대 형성입니다.

세 번째 섹션은 치료 종류입니다. 하나의 치료 안에도 여러 세부 옵션이 있음을 보여줍니다. "여기는 단순히 치료를 하는 곳이 아니라, 내 상황에 맞는 최적의 방법을 찾아주는 곳"이라는 전문성을 어필합니다.

네 번째 섹션은 차별점입니다. "왜 `{병원명}`에서 치료받아야 하는가?"에 대한 직접적인 답변입니다. 4~6개 포인트로 구성하며, `{차별점1}`, `{차별점2}`, `{차별점3}` 등 병원의 핵심 강점을 내세웁니다.

다섯 번째 섹션은 치료 과정(HowTo)입니다. 환자가 가장 불안해하는 것 중 하나가 "뭘 하는지 모르겠다"는 것입니다. 치료 과정을 단계별로 명확하게 보여주면 불안이 크게 줄어듭니다. 이 섹션은 Schema.org의 HowTo 스키마로 마크업되어 구글 검색 결과에서 단계별 리치스니펫으로 노출될 수 있습니다.

여섯 번째 섹션은 담당 의료진입니다. 해당 치료를 전문으로 하는 의사 2~3명의 사진, 이름, 전문분야, 한 줄 소개를 보여주고, 각 의사 프로필 페이지로 링크합니다.

일곱 번째 섹션은 FAQ입니다. 치료별로 5~8개의 자주 묻는 질문을 아코디언 형태로 배치합니다. 이 FAQ는 Schema.org의 FAQPage 스키마로 마크업되어 구글 검색 결과에서 직접 펼쳐져 보일 수 있습니다.

마지막으로 최종 CTA 섹션에서 "지금 바로 무료 상담 예약"과 비용 안내 페이지 링크를 제공합니다.

## 3.3 의사 프로필 — 환자가 가장 궁금해하는 "이 사람"

환자에게 가장 중요한 결정 요인 중 하나가 "누가 나를 치료하는가"입니다. 특히 장기간 관계를 맺는 치료에서는 의사에 대한 신뢰가 병원 선택을 좌우합니다.

의사 프로필 페이지의 필수 요소는 다음과 같습니다. 고화질 전문 촬영 사진(핸드폰으로 찍은 사진은 절대 안 됩니다), 이름과 직함("`{대표원장}` 대표원장"), 학력, 자격증 및 전문의, 주요 경력(연도순), 전문 분야, 소속 학회와 논문 실적, 소개 영상(유튜브 임베드 — 2분 이내로 자신의 진료 철학을 말하는 영상), 실제 진료 모습 사진, 담당 치료 링크(이 의사가 전문으로 하는 치료 페이지로 바로 이동), 그리고 인간적 스토리까지.

인간적 스토리가 중요한 이유는, 환자가 단순히 "실력 있는 의사"가 아니라 "나를 이해해줄 것 같은 의사"를 찾기 때문입니다. 학력이나 경력보다 더 강한 신뢰를 만드는 경우가 많습니다.

## 3.4 지역 SEO 페이지 — 검색 유입의 숨은 병기

지역 SEO 페이지는 환자가 직접 보기보다는, 검색엔진을 통해 유입되게 하는 전략 페이지입니다. "`{서비스지역의 다른 도시}` `{진료과}`" 같은 키워드로 검색했을 때, 메인 페이지가 아닌 해당 지역 전용 랜딩 페이지가 뜨면 환자의 관련성 인식이 높아집니다.

각 지역 페이지의 구조는 동일하지만, 내용은 지역별로 완전히 다릅니다. 히어로에서 "{지역}에서 `{병원명}`까지 차로 {N}분"이라는 구체적 소요시간을 제시하고, 왜 이 병원인가 섹션에서 차별점 6~8개를 나열하고, 대표 치료 4~6개를 소개하고, 교통 안내 섹션에서 해당 지역에서 오는 구체적인 경로를 안내하고, 실제 해당 지역에서 온 환자의 리뷰를 보여주고, 구글/네이버 맵을 임베드하고, 마지막으로 예약 CTA를 배치합니다.

## 3.5 예약/상담 페이지 — 전환의 최종 관문

예약 페이지의 설계 원칙은 "최소한의 마찰로 최대한의 전환을"입니다. 먼저 빠른 연락 수단 3종(전화 `{전화번호}`, 카카오톡 `{카카오톡URL}`, 네이버 예약 `{네이버예약URL}`)을 페이지 최상단에 크게 배치합니다. 환자의 절반 이상이 온라인 폼보다 전화나 카카오톡을 선호한다는 데이터에 기반합니다.

온라인 예약 폼은 단계별로 설계합니다. Step 1에서 치료 종류를 선택하고, Step 2에서 이름, 연락처, 희망 날짜/시간대, 특이사항을 입력하고, Step 3에서 확인 후 제출합니다. 한 화면에 모든 필드를 보여주는 것보다, 단계별로 나누는 것이 완료율이 2~3배 높습니다.

진료시간 정보를 반드시 이 페이지에 포함시킵니다: 평일 `{진료시간_평일}`, 토요일 `{진료시간_토}`, 일요일 `{진료시간_일}`, 공휴일 `{진료시간_공휴일}`.

## 3.6 비용 안내 페이지 — 투명성이 곧 신뢰

비용 안내는 대부분의 병원이 꺼리지만, 공개하는 것이 무조건 유리합니다. 이유는 세 가지입니다. 첫째, 환자의 90%가 비용을 가장 먼저 궁금해합니다. 둘째, 비용을 숨기면 "비싸니까 숨기는 거겠지"라는 의심을 삽니다. 셋째, 의료법 시행규칙 제42조의2에 따라 비급여 진료비용은 홈페이지에 공개해야 합니다 — 법적 의무이기도 합니다.

가격은 정확한 금액이 아닌 범위로 표시합니다. 보험 적용 여부를 구분하고, "정확한 비용은 진단 후 상담을 통해 안내해 드립니다"라는 법적 고지를 포함합니다. 카드 결제와 할부 옵션도 안내합니다.

## 3.7 비포/애프터 갤러리 — 실력의 증거

"백 마디 말보다 한 장의 사진"입니다. 갤러리 페이지에는 카테고리 필터(`{주력치료}` 기준), 좌우 드래그 비교 슬라이더, 케이스 상세 정보(치료 기간, 담당 의사, 간략한 설명), 담당 의사 프로필 링크, "총 N건의 치료 사례" 통계를 포함합니다.

의료법 주의사항: 환자 서면 동의는 법적 필수이며, 동일 조건(조명, 각도)에서 촬영해야 합니다. "개인에 따라 결과가 다를 수 있습니다"라는 고지를 반드시 포함하고, 과장이나 허위 사례는 절대 금지입니다.

---

# 4. UI/UX 설계 기준

## 4.1 디자인 시스템 — 일관성이 전문성을 만든다

병원 홈페이지의 디자인은 "예쁘게"가 아니라 "신뢰감 있게"가 핵심입니다. 화려한 애니메이션이나 트렌디한 레이아웃보다, 깔끔하고 정돈되고 읽기 쉬운 디자인이 의료기관에 훨씬 적합합니다.

**컬러 팔레트**는 6가지 역할로 나눕니다. 프라이머리 컬러 `{프라이머리컬러}`는 `{병원명}`의 브랜드 아이덴티티를 담습니다. 세컨더리 컬러 `{세컨더리컬러}`는 보조/링크색으로 활용합니다. 배경색은 `{다크배경}`(다크모드) / #FFFFFF(라이트모드), 텍스트 컬러는 메인(#1a1a1a)/서브(#666666)로 가독성을 확보하고, CTA 컬러는 프라이머리와 동일하게 눈에 띄게, 상태 컬러는 성공(녹)/경고(노)/에러(적)입니다. 컬러 선택의 원칙은 "차분하되 한 가지 포인트 컬러로 고급감을 준다"입니다. `{브랜드톤}`에 맞추어 설정합니다.

**타이포그래피**는 한글 폰트로 Pretendard(CDN 로딩)를 사용합니다. H1은 Bold 36~48px(페이지당 1개만), H2는 Bold 28~36px(섹션 타이틀), H3는 Semi-Bold 20~24px(서브 타이틀), 본문은 Regular 15~16px에 line-height 1.7로 설정합니다.

**아이콘**은 Font Awesome 6.4를 CDN으로 로딩합니다. 로고와 같은 고유 아이콘만 커스텀 SVG로 제작합니다.

## 4.2 반응형 — 모바일이 75%, 무조건 모바일 퍼스트

2026년 현재 병원 홈페이지 트래픽의 75% 이상이 모바일에서 발생합니다. 반응형 브레이크포인트는 모바일(~767px, 1열 레이아웃 + 하단 고정 CTA), 태블릿(768~1023px, 2열), 데스크톱(1024px~, 3~4열 + 플로팅 CTA) 3단계로 설정합니다.

모바일 퍼스트 필수 요소: 하단 고정 CTA 바(전화/카카오/예약/길찾기 4버튼), 터치 타겟 최소 44×44px, 전화번호 `tel:` 링크 필수, 폰트 최소 14px(권장 16px), 이미지 WebP + lazy loading, 스크롤 애니메이션은 과도하지 않게.

## 4.3 접근성 (A11y)

모든 인터랙티브 요소에 ARIA 라벨을 부여합니다. 키보드 네비게이션 Tab 순서 보장(시맨틱 HTML), 색상 대비 WCAG 2.1 AA 기준 4.5:1 이상, 모든 이미지에 alt 텍스트, 키보드 포커스 시각적 표시(:focus-visible), 페이지 상단에 "본문 바로가기" 건너뛰기 링크를 넣습니다.

---

# 5. SEO 인프라

## 5.1 온페이지 SEO — 모든 페이지의 DNA

SEO는 "검색엔진 최적화"이지만, 본질은 "환자가 검색했을 때 `{병원명}`이 나오게 하는 것"입니다. 모든 페이지에 동일한 SEO 인프라가 내장되어야 합니다.

**HTML Head 필수 태그 14종**을 하나씩 설명합니다.

`<title>` 태그는 검색 결과에서 가장 크게 보이는 텍스트입니다. 60자 이내로 "{페이지 제목} | `{병원명}`" 형식을 씁니다. 모든 페이지 고유한 타이틀이어야 합니다.

`<meta description>` 은 검색 결과에서 타이틀 아래에 표시되는 설명문입니다. 150자 이내로 키워드와 행동 유도를 함께 넣습니다.

`<link rel="canonical">`은 해당 페이지의 정규 URL `https://{도메인}/{경로}`을 선언합니다.

`<meta name="robots">`로 "index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1"을 설정합니다.

Geo 태그 3종 — `geo.region`에 `{지역코드}`, `geo.placename`에 "`{도시명}`, `{도명}`", `geo.position`에 "`{위도}`;`{경도}`"를 지정합니다. 지역 SEO의 핵심입니다.

AI 검색 최적화 태그(ai.summary)는 ChatGPT, Gemini, Perplexity 등 AI 검색 엔진이 참조하는 200자 이내 요약입니다.

Open Graph 태그 6종(og:type, og:site_name에 `{병원명}`, og:title, og:description, og:url, og:image)은 카카오톡, 페이스북 등에서 URL 공유 시 미리보기를 결정합니다. og:image는 1200×630px 표준입니다.

Twitter Card 태그 3종(twitter:card, twitter:title, twitter:description, twitter:image)은 트위터/X에서의 미리보기를 결정합니다.

검색엔진 인증 메타태그(google-site-verification, naver-site-verification)는 도메인 소유권 증명입니다.

추가로 H1 태그는 페이지당 정확히 1개만, H2~H3는 계층적으로, 모든 이미지에 alt 태그, 내부 링크 적극 사용, HTML lang="ko" 설정합니다.

## 5.2 기술적 SEO

**sitemap.xml**은 사이트의 모든 URL을 검색엔진에 알려주는 지도입니다. `{병원명}` 기준 `{사이트맵URL수}`개 URL을 포함합니다. 메인 페이지 priority 1.0, 치료 0.9, 의사 0.8, 지역 0.7, 콘텐츠 0.6, 법적 0.3으로 설정합니다.

**robots.txt**는 관리자(/admin/), 회원(/auth/), API(/api/), 워커(/_worker.js)를 차단하고 나머지를 허용합니다.

**IndexNow 키 파일**은 Bing과 네이버에 새 페이지를 즉시 알려주는 메커니즘입니다.

**URL 설계 원칙**은 영문 소문자, 하이픈 구분, .html 확장자 제거(Clean URL), 짧고 명확, 계층적, 한글 금지입니다. `/treatments/implant`는 좋은 URL, `/Treatments/Implant.html`은 나쁜 URL입니다.

---

# 6. 구조화 데이터 (Schema.org)

## 6.1 구조화 데이터가 왜 중요한가

구조화 데이터는 검색엔진에게 "이 페이지가 무엇에 관한 것인지"를 기계가 이해할 수 있는 형태로 알려주는 코드입니다. JSON-LD 형식으로 HTML에 삽입하며, 검색 결과에서 리치스니펫(별점, FAQ 펼침, 가격 범위, 치료 과정 등)으로 표시됩니다.

`{병원명}`은 `{총페이지수}`개 페이지에 `{JSON-LD수}`개의 JSON-LD 항목과 30종 이상의 스키마 유형을 사용합니다.

## 6.2 페이지별 필수 스키마

**모든 페이지 공통 4가지**: Dentist(병원 기본 정보 — 지식패널, 지도 노출), BreadcrumbList(경로 표시), WebSite + SearchAction(검색창 표시), SpeakableSpecification(음성 검색 대응).

**치료 페이지 전용 8가지**: MedicalWebPage, MedicalProcedure, MedicalTherapy, HowTo + HowToStep, FAQPage, AggregateOffer, AggregateRating, Product.

**의사 페이지 전용**: Person, EducationalOccupationalCredential, CollegeOrUniversity, Organization.

**지역 페이지 전용**: Dentist + areaServed, City, OpeningHoursSpecification.

## 6.3 Dentist 스키마 핵심 필드

Dentist 스키마에 `{병원명}`, `{병원명_영문}`, `https://{도메인}/`, `{전화번호}`, 대표 이미지, 로고, `{주소}`(PostalAddress), `{위도}`/`{경도}`(GeoCoordinates), 진료시간(OpeningHoursSpecification — `{진료시간_평일}`, `{진료시간_토}`, `{진료시간_일}`, `{진료시간_공휴일}` 매핑), `{구글평점}`/`{리뷰수}`(AggregateRating), 외부 프로필(sameAs — `{네이버예약URL}`, `{유튜브URL}`, `{카카오톡URL}`), 서비스 지역(`{서비스지역목록}` 각각을 City로), 가격대(priceRange)를 담습니다.

규모별 적정 JSON-LD 항목 수: 소규모(20페이지) 40~60개, 중규모(80페이지) 130~150개, 대규모(200페이지+) 300개+.

---

# 7. 마케팅 & 전환 인프라

## 7.1 필수 마케팅 도구

**Google Tag Manager(GTM)** — 모든 추적 코드를 한 곳에서 관리하는 컨테이너. `<head>`와 `<body>`에 스니펫 삽입.

**Google Analytics 4(GA4)** — 트래픽 분석의 표준. GTM 경유 관리.

**Amplitude** — GA4보다 정밀한 사용자 행동 분석. "환자가 어떤 순서로 페이지를 이동하는가?", "예약을 어느 단계에서 포기하는가?" 분석.

**Google Search Console** — 구글 검색 성능 모니터링. **Naver Search Advisor** — 네이버 동일 역할. **Bing Webmaster** — Bing + IndexNow.

## 7.2 이벤트 트래킹 — 9가지 핵심 이벤트

reservation_click(예약 클릭 — 어느 페이지, 어떤 소스), phone_click(전화 클릭 — 모바일 최고 전환), kakao_click(카카오톡 클릭), map_click(지도/길찾기), treatment_view(치료 페이지 조회 — 어떤 치료에 관심), doctor_view(의사 페이지 조회), pricing_view(비용 페이지 — 체류시간 길수록 구매 의향 높음), scroll_depth(25/50/75/100%), cta_click(CTA 유형/위치별 효과 분석).

## 7.3 전환 최적화

CTA 우선순위: 전화 → 카카오톡 → 네이버 예약 → 온라인 폼 → 길찾기.

**구글 비즈니스 프로필 연동**: Google Places API로 실시간 리뷰 로딩(/api/google-reviews), 메인 페이지에 `{구글평점}` / `{리뷰수}` 표시, "구글 리뷰 남기기" 버튼으로 신규 리뷰 유도.

## 7.4 콘텐츠 시스템

`{블로그URL}`의 RSS를 프록시하여(/api/inblog-rss) 칼럼 페이지에 최신 글 표시. `{유튜브URL}`의 RSS를 프록시하여(/api/youtube-rss) 영상 페이지에 최신 영상 표시. 병원이 평소대로 블로그/유튜브를 올리면 홈페이지가 자동 업데이트됩니다.

---

# 8. 다국어 지원

## 8.1 왜 다국어가 필요한가

한국에 거주하는 외국인은 250만 명을 넘어섰고, 특히 산업단지가 있는 지역에서는 외국인 환자 비율이 높습니다. `{병원명}`이 위치한 `{도시명}` 주변의 외국인 인구 분포를 분석하여 최적의 언어를 선정합니다. 외국인 환자에게 모국어로 정보를 제공하는 병원은 거의 없습니다 — 그 자체가 압도적 차별화입니다.

## 8.2 Weglot — 1줄의 코드로 `{다국어수}`개 언어

Weglot은 JavaScript 1줄만 삽입하면 전체 사이트를 자동 번역해주는 서비스입니다. URL 구조가 서브디렉토리 방식(/en/, /vi/, /zh/ 등)으로 자동 생성되고, 언어 선택기가 자동으로 나타나며, Visual Editor로 번역을 수동 수정할 수 있습니다.

`{병원명}`의 언어 전략: `{다국어목록}` — `{다국어수}`개 언어. `{Weglot플랜}` 플랜 사용.

**용어집 설정**이 중요합니다. `{병원명}` → `{병원명_영문}`, `{자체브랜드치료}` (있는 경우 원어 유지), 전화번호 `{전화번호}` (번역 금지), 주소 `{주소}` (로마자 표기 지정)를 용어집에 등록합니다.

---

# 9. 기술 스택 & 아키텍처

## 9.1 왜 Hono + Cloudflare Pages인가

병원 홈페이지의 기술 스택 선택 기준은 "빠르고, 안정적이고, 관리 비용이 거의 없는 것"입니다. 워드프레스는 플러그인 충돌, 보안 취약점, 서버 관리 문제가 있고, Wix나 Squarespace는 커스터마이징/SEO에 한계가 있습니다.

Hono는 Cloudflare Workers에서 동작하는 경량 웹 프레임워크(번들 12KB 미만), Cloudflare Pages는 전 세계 300개+ 데이터 센터 CDN(무료 플랜 — 무제한 요청/대역폭), Vite는 빌드 도구입니다.

장점: 월 호스팅 비용 $0, 글로벌 CDN, 서버 관리 불필요, DDoS 방어/SSL 자동, 배포 1줄(`npm run deploy`).

## 9.2 프로젝트 구조

`src/index.tsx`가 Hono 백엔드(API + 라우팅), `public/` 아래에 모든 정적 파일(HTML, CSS, JS, 이미지). HTML은 treatments/, doctors/, area/, cases/, column/, video/, notice/, auth/, admin/으로 분류. CSS는 단일 파일(site-v5.css) 통합. JavaScript는 기능별(main.js, gnb.js, analytics.js 등) 분리. 이미지는 doctors/, facility/, icons/로 분류.

## 9.3 백엔드 API 라우트

`/api/health`(헬스체크), `/api/google-reviews`(구글 리뷰 프록시 — `{구글API키}` 서버사이드 보호), `/api/inblog-rss`(블로그 RSS), `/api/youtube-rss`(유튜브 RSS), `/api/chat`(AI 챗봇 — 선택), `/api/indexnow`(즉시 색인), `/api/google-ping`(사이트맵 갱신 알림).

## 9.4 호스팅 비용

Cloudflare Pages 무료 플랜: 무제한 요청, 무제한 대역폭, 월 500회 빌드. `{총페이지수}`페이지 규모는 무료 플랜으로 충분. 도메인 비용(연 2~3만원)과 Weglot(`{Weglot플랜}`)이 사실상 유일한 운영 비용입니다.

---

# 10. 검색엔진 등록 & 색인

## 10.1 Google 등록

Google Search Console에서 HTML 메타태그로 `{도메인}` 소유권 인증 → 사이트맵 제출 → 메인 페이지 URL 검사 → 색인 생성 요청 → 구조화 데이터 확인. 메인 1~3일, 치료 3~7일, 전체 2~4주.

## 10.2 Naver 등록

Naver Search Advisor에서 인증 → 사이트맵 제출 → RSS 제출(블로그 연동 시) → 핵심 URL 수동 수집 요청 → robots.txt 검증. 색인 3~14일.

## 10.3 Bing + IndexNow

Bing Webmaster를 GSC 연동으로 등록. IndexNow 키 파일 배포 + /api/indexnow로 `{사이트맵URL수}`개 URL 전송. Bing/Naver/Yandex에 수분 내 색인.

## 10.4 모니터링

메인 색인 1~3일, 치료 색인 3~7일, 구조화 데이터 오류 7일 내 0 확인, 네이버 3~14일, Bing 1~3일.

---

# 11. 법적 필수 요소

## 11.1 의료광고 규정

의료법 56조 준수 필수. 광고 사전 심의/면제 확인, 전후 사진 동일 조건+환자 동의, 비급여 가격 공개(의료법 시행규칙 42조의2), "최고/100%/완벽" 등 과장 금지, "개인에 따라 결과 다를 수 있음" 고지 필수, 의사 경력 정확 기재.

모든 치료 페이지 하단에 고지문: "본 홈페이지의 모든 의료 정보는 의료법 및 보건복지부 의료광고 가이드라인을 준수하여 제공하고 있으며, 특정 개인의 결과는 개인에 따라 달라질 수 있습니다."

## 11.2 개인정보보호

개인정보처리방침 홈페이지 게시(개인정보보호법 30조), 예약/회원가입 시 수집 동의(15조), 마케팅 수신 별도 선택 동의(정보통신망법 50조), 다국어 사이트는 GDPR 쿠키 배너 고려.

개인정보처리방침 필수 10항목: 수집 항목, 수집·이용 목적, 보유·이용 기간, 제3자 제공, 처리 위탁, 정보주체 권리, 파기 절차, 보호책임자, 쿠키, 권익침해 구제.

## 11.3 기타 법적 요소

푸터에 사업자 정보(`{병원명}`, `{대표원장}`, 사업자등록번호, `{주소}`), 저작권("© 2026 `{병원명}`"), 치료 페이지 면책 고지.

---

# 12. 성능 & 보안

## 12.1 Core Web Vitals

**LCP** < 2.5초(히어로 이미지 WebP + preload), **FID** < 100ms(JS async/defer), **CLS** < 0.1(이미지/광고 크기 명시, 폰트 최적화).

성능 체크리스트: 이미지 WebP + lazy loading, CSS/JS 최소화(Vite), 폰트 preconnect + preload, CDN(Cloudflare 자동), 캐싱(Cache-Control), 크리티컬 CSS 인라인, JS 비동기, prefetch.

## 12.2 보안

HTTPS + SSL(Cloudflare 자동), DDoS(Cloudflare 자동), 보안 헤더(CSP, X-Frame-Options), API 키 서버사이드만(`{구글API키}` 등 프론트엔드 노출 금지), 폼 데이터 서버측 검증.

---

# 13. 납품 체크리스트

## 13.1 Phase 1: 기본 구축 (2~3주)

도메인 `{도메인}` 연결(Cloudflare DNS), 메인 페이지, 예약 페이지(`{전화번호}`, `{카카오톡URL}`, `{네이버예약URL}` 연동), 비용 안내, 오시는 길(`{주소}`, `{위도}`/`{경도}` 지도), 의료진 목록, `{대표원장}` 프로필, 주력 치료 5종(`{주력치료}`), 개인정보처리방침, 이용약관, 404, 전 페이지 모바일 반응형, 기본 SEO, robots.txt, sitemap.xml, 파비콘, SSL, Git. **18개 항목.**

## 13.2 Phase 2: 확장 구축 (2~3주)

나머지 치료 전체 `{치료페이지수}`개 완성, 의사 프로필 `{의사프로필수}`개 전체, 지역 SEO `{지역페이지수}`개, FAQ, 층별 안내(`{시설규모}` 반영), 미션/비전, 칼럼(RSS), 영상(유튜브 RSS), 공지사항, 구조화 데이터 `{JSON-LD수}`개, 비포/애프터 갤러리, 회원 시스템, PWA manifest. **13개 항목.**

## 13.3 Phase 3: SEO & 마케팅 (1~2주)

GTM(전 페이지), GA4, Amplitude, Google Search Console(`{도메인}` 인증 + 사이트맵), Naver Search Advisor, Bing Webmaster + IndexNow(`{사이트맵URL수}`개 URL), 구글 리뷰 위젯(`{구글PlaceID}` + `{구글API키}`), Open Graph 이미지. **9개 항목.**

## 13.4 Phase 4: 다국어 & 고급 (1주)

Weglot 설치, `{다국어목록}` `{다국어수}`개 언어 설정, 용어집(`{병원명}` → `{병원명_영문}`, `{자체브랜드치료}` 등), 번역 검수, 관리자 CMS. **5개 항목.**

## 13.5 최종 검수 — 28개 항목

기능 6개(링크, 예약 폼, 전화 `{전화번호}`, 카카오 `{카카오톡URL}`, 네이버 `{네이버예약URL}`, 지도), 반응형 3개(375/768/1440px), SEO 7개(title/description/canonical 고유, sitemap `{사이트맵URL수}` 일치, robots 경고 0, JSON-LD 유효, GSC 오류 0), 성능 2개(Lighthouse 90+, 이미지 최적화), 보안 2개(HTTPS, API 키 미노출), 법적 4개(개인정보처리방침, 이용약관, 의료광고 고지문, 사업자 정보), 분석 2개(GA4 수집, GTM 발화), 다국어 2개(Weglot 로딩, 언어 전환).

---

# 14. 운영 가이드

## 14.1 홈페이지는 만들고 끝이 아니다

**매일**: 예약 확인/응대. **매주**: 블로그/칼럼 1개+(InBlog/네이버), GBP 게시물 1개. **매월**: 비포/애프터 2~3건, 가격 변동 확인. **분기**: SEO 리포트(GSC), Core Web Vitals 점검. **반기**: 개인정보처리방침 갱신. **연 1회**: 저작권 연도 업데이트.

## 14.2 병원에서 직접 관리 가능한 것

블로그 작성(InBlog/네이버 ★☆☆), 유튜브 업로드(★☆☆), 공지사항(CMS ★☆☆), 리뷰 응답(★☆☆), GBP 게시물(★★☆), Weglot 번역 수정(★★☆), 예약 확인(★☆☆).

## 14.3 개발사에 요청해야 하는 것

신규 의사 추가(비정기 ★★☆), 신규 치료 페이지(비정기 ★★★), 가격 수정(분기 ★☆☆), 비포/애프터 추가(월 ★★☆), 지역 페이지 추가(비정기 ★★☆), SEO/스키마 수정(분기 ★★★), 디자인 수정(비정기 ★★★).

---

# 15. 부록: 커스터마이징 옵션

## 15.1 추가 기능

**바이럴 퀴즈** — `{진료과}`에 맞는 재미있는 테스트(예: "나의 치아 캐릭터는?"). 결과 카드 SNS 공유로 바이럴 유도 + 예약 CTA. **비용 계산기** — 조건 선택 → 예상 비용 → 전화번호 입력으로 리드 확보. **AI 챗봇** — 24시간 자동 상담. **카카오 알림톡** — 예약 확인/리마인더 자동 발송 → 노쇼 방지.

## 15.2 과별 특화 요소

**치과**: 수술실 `{수술실수}`개 규모 강조, 디지털 장비, 수면치료/무통, 소아치과 분리. **피부과/성형외과**: 비포/애프터 중심, 가격 비교표, 1:1 상담, SNS(인스타그램) 연동. **한의원**: 한방 원리 설명, 증상별 페이지, 체질 진단 퀴즈, 건강보험 안내.

## 15.3 가격 패키지

**Basic** (~15페이지): 메인 + 예약 + 비용 + 오시는 길 + 치료 5개 + 의사 3명. 최소한의 온라인 존재감.

**Standard** (~40페이지): Basic + 치료 15개 + 전체 의사 + 지역 5개 + FAQ + 갤러리. SEO와 신뢰 구축 가능.

**Premium** (~80페이지): 전체 + SEO(`{JSON-LD수}`개 JSON-LD) + GTM/GA4 + 다국어(`{다국어수}`개) + 리뷰 위젯. 풀스펙.

**Enterprise** (100페이지+): Premium + AI 챗봇 + CMS + 바이럴 퀴즈 + 비용 계산기 + 월 유지보수.

---

# 부록 A: 클라이언트 정보 수집 양식

> 아래 항목을 수집하여 상단 **변수 정의표**에 채워넣으면 기획서가 완성됩니다.

**기본 정보**: 병원명(한글/영문), 대표원장, 전화번호, 주소, 우편번호, 위도/경도, 도메인, 기존 홈페이지 URL

**진료 정보**: 진료과, 전체 치료/진료 목록, 주력 치료 TOP 5, 치료 카테고리 분류, 자체 브랜드 치료(있는 경우), 진료시간(평일/토/일/공휴일/야간), 365일 진료 여부

**차별점**: 핵심 차별점 3~6개, 시설 규모(평수), 수술실/시술실 수, 특수 장비·시스템

**의료진**: 의사 수, 각 의사별(이름, 직함, 전문분야, 학력, 경력, 사진)

**마케팅/소셜**: 네이버 예약 URL, 카카오톡 채널 URL, 유튜브 채널 URL, 인스타그램 URL, 블로그 URL, Google Place ID, Google API Key, 네이버 평점, 구글 평점, 총 리뷰 수

**지역 SEO**: 타겟 지역 리스트(차로 30분~1시간 이내), 지역 페이지 수

**다국어**: 번역 언어 목록, Weglot 플랜

**디자인**: 브랜드 컬러(메인/보조), 다크 배경색, 로고 파일, 브랜드 톤(프리미엄/모던/따뜻한/전문적), 메인 카피, 서브 카피

---

# 부록 B: `{병원명}` 수치 요약

| 항목 | 값 |
|------|-----|
| 총 HTML 페이지 | `{총페이지수}`개 |
| 치료 페이지 | `{치료페이지수}`개 (+ 목록 1개) |
| 의사 프로필 | `{의사프로필수}`개 (+ 목록 1개) |
| 지역 SEO 페이지 | `{지역페이지수}`개 |
| JSON-LD 스키마 항목 | `{JSON-LD수}`개 |
| 스키마 유형 | 30종+ |
| sitemap URL | `{사이트맵URL수}`개 |
| API 라우트 | 8개 |
| 다국어 | `{다국어수}` + 1(원어) = `{총언어수}`개 |
| 월 호스팅 비용 | $0 (Cloudflare Pages Free) |
| 배포 방식 | Cloudflare Pages (Edge CDN) |
| 도메인 | `{도메인}` |

---

> **이 기획서는 서울비디치과(bdbddc.com) 실제 운영 데이터를 기반으로 제작된 범용 템플릿입니다.**
> **Patient Funnel (페이션트 퍼널) 독점 자료입니다.**
>
> 문의: Patient Funnel
> 레퍼런스: https://bdbddc.com

---

*v3.0 — Template Edition | 2026-02-19 | Patient Funnel*
